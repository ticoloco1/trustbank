export const metadata = {
  title: "Videos & paywall - TrustBank",
  description: "Sign in with Google to add videos and enable paywall.",
};

export default function SiteEditLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
